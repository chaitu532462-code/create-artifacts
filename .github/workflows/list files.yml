name: Listfiles

on: [push, workflow_dispatch]

jobs:
  files_data:
    name: list files
    runs-on: ubuntu-latest
    steps:
      - name: list files
        run: pip install python

      - name: check pyhton
        run: python --version