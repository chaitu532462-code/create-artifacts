name: Listfiles

on: [push, workflow_dispatch]

jobs:
  files_data:
    name: list files
    runs-on: ubuntu-latest
    steps:
      - name: list files
        run: pip intsall python

      - name: check pyhton
        run: python --version  